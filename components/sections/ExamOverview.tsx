const fb={parts:[{name:"Hoeren (Listening)",duration:"20 min",tasks:3,points:30},{name:"Lesen (Reading)",duration:"25 min",tasks:3,points:30},{name:"Schreiben (Writing)",duration:"20 min",tasks:2,points:30},{name:"Sprechen (Speaking)",duration:"15 min",tasks:3,points:30}],passMark:"60 out of 120 points (50%)",tip:"Each part worth 30 pts. Need 50% to pass."}
export default function ExamOverview({data}:{data?:any}){const d=data?.fields||fb;const parts=d.parts||fb.parts
return(<section id="exam" className="max-w-6xl mx-auto px-6 py-16 section-fade"><h2 className="text-3xl font-bold mb-2">Exam Overview</h2><p className="text-gray-500 mb-8">TELC Deutsch A1 - What to expect</p><div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">{parts.map((p:any)=>(<div key={p.name} className="bg-blue-50 border border-blue-100 rounded-2xl p-5"><p className="font-bold text-blue-800 mb-1">{p.name}</p><p className="text-sm text-gray-600">{p.duration} - {p.tasks} tasks</p><p className="text-sm font-semibold text-blue-700 mt-2">{p.points} pts</p></div>))}</div><div className="bg-green-50 border border-green-200 rounded-2xl p-5 text-green-800"><p className="font-bold mb-1">Pass Mark: {d.passMark||fb.passMark}</p><p className="text-sm">{d.tip||fb.tip}</p></div></section>)}
